__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
# ...but keep the built web UI (committed for zero-build deployments)
!web/dist/
!web/dist/**
.pytest_cache/
.venv/
venv/
.env
*.jsonl
/tmp/
.DS_Store

# Local runtime state (created by taiyi serve --base-dir state)
state/

# Web build tooling (regenerated from package.json; not committed)
web/node_modules/
